fixed_args:
    options:
        '--num_epochs': 100
        '--batch_size': 128
        '--data_root': '/media/HDD/ImageNet/ImageNet/imagenet'
        '--pretrained': True
        '--device': 'cuda'
        '--storage_path': './results'
        '--num_workers': 12
        '--prefetch_factor': 4
        '--model': 'densenet121'
        

varying_args:
    range:
        options:
            '--seed': [0, 11, 1]

    # groups:
    #     - options:
            
        
    #     - options:
    #         '--model': 'mobilenet_v2'
        
    #     - options:
    #         '--model': 'efficientnet_b0'
