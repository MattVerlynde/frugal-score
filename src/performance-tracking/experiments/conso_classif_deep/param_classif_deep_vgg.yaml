fixed_args:
    options:
        '--num_epochs': 20
        '--batch_size': 256
        '--data_root': './data'
        '--pretrained': False
        '--device': 'cuda'
        '--storage_path': './results'
        '--num_workers': 12
        '--prefetch_factor': 4
        '--model': 'vgg16'
        

varying_args:
    range:
        options:
            '--seed': [0, 11, 1]

    # groups:
    #     - options:
            
        
    #     - options:
    #         '--model': 'mobilenet_v2'
        
    #     - options:
    #         '--model': 'efficientnet_b0'
