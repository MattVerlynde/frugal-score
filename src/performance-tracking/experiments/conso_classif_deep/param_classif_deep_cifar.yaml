fixed_args:
    options:
        '--num_epochs': 500
        '--batch_size': 256
        '--data_root': './data/cifar'
        '--pretrained': False
        '--device': 'cuda'
        '--storage_path': './results'
        '--num_workers': 12
        '--prefetch_factor': 4
        # '--model': 'resnet18'
        '--seed': 1
        

varying_args:
    # range:
    #     options:
    #         '--seed': [0, 10, 1]

    groups:
        - options:
            '--model': 'resnet18'
        
        - options:
            '--model': 'resnet50'
        
        - options:
            '--model': 'vgg16'
        
        - options:
            '--model': 'densenet121'
        
        - options:
            '--model': 'mobilenet_v2'
        
        - options:
            '--model': 'efficientnet_b0'
        
        - options:
            '--model': 'googlenet'
        
        - options:
            '--model': 'shufflenet_v2_x0_5'
        
        - options:
            '--model': 'squeezenet1_0'
        
    #     - options:
    #         '--learning_rate': 0.01
